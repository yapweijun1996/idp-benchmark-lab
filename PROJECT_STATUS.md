# PROJECT_STATUS — IDP Benchmark Lab

## Decision

**NO-GO for production; local remediation and the PWA/i18n/Gateway Demo change set pass local checks, while full live-provider evidence is still open.** The last verified published checkpoint is `1b76da1`; its Actions run [34214400653](https://github.com/yapweijun1996/idp-benchmark-lab/actions/runs/34214400653) passed the full lint/typecheck/unit/build/audit/browser gate, and the Pages deployment is live. A current Pages-origin spike confirms the Gateway Demo accepts bounded `input_image` Responses requests and streams usage, and a deployed-browser run confirms the frontend map/reducer path. A real PDF map/reduce run and quota/billing reconciliation remain required before GO.

The app remains a static Pages/PWA with direct provider adapters and no server. Home → New Benchmark is the active six-step flow. Dedicated keys and custom headers are ephemeral; suite evidence freezes effective inputs, versions, pricing and build identity. Strict/normalized metrics remain separate. All retained history is visible; the canonical i18n registry covers literal UI calls in English, Mandarin, Malay, Japanese, and Vietnamese, with explicit English fallback for data-derived technical values. The update prompt shows the loaded friendly version and waits for explicit acceptance; About exposes the full build identity.

New browser profiles are seeded once with a keyless `Gateway Demo` provider configuration, so the active wizard has a selectable default even before Settings is opened. The first Test or Run automatically obtains the short-lived session when needed; the explicit Connect action remains available for an immediate connection or manual reconnect, and removing the default is not undone by subsequent fixture mounts.

An earlier external check on 2026-09-08 recorded published checkpoint `dd98943`. Actions run [34207495290](https://github.com/yapweijun1996/idp-benchmark-lab/actions/runs/34207495290) passed all required checks, including Chromium, Firefox and WebKit browser acceptance, and the Pages deployment reports success at `https://yapweijun1996.github.io/idp-benchmark-lab/`. The live origin returns HTTP 200 and the current wizard/update UI. A fresh Pages-origin spike obtained `/demo/session` with `201` and a `dmo_`-shaped token, `/demo/v1/models` with `200`, Responses preflight with `204` and exact `Access-Control-Allow-Origin`, one-image JSON `200`, four-image streaming `200` with usage, and five-image `400` `DEMO_IMAGE_COUNT_EXCEEDED`. No token was retained in output. This proves the bounded image route and CORS contract; it does not yet prove a real PDF map/reduce or quota reconciliation.

A real-browser check of the un-deployed branch from `http://127.0.0.1:5173` was kept separate from the Pages-origin evidence: the gateway returned `403` for `/demo/session` because that development Origin is not registered. The UI surfaced the origin/session boundary and retained no session token in `sessionStorage`. This is an expected gateway policy result, not a model failure; live browser acceptance must run from the registered Pages origin or after the gateway owner registers a development origin.

A Chromium run against the published Pages origin exercised the deployed Gateway Demo path with a synthetic five-page PDF. The browser received session `201`, then three Responses SSE calls with status `200` and usage: map batches containing 4 and 1 images followed by one text-only reducer call. The Quick Test completed and no `dmo_`, `gw_`, or `sk_` value appeared in `sessionStorage`. This proves the published map/reducer wiring for synthetic input; a real user PDF, quota/billing reconciliation, and target-device storage evidence remain open.

A follow-up Pages-origin replay using the bundled purchase-order page (1191×1684 PNG) reproduced the gateway's `400 DEMO_INPUT_TOO_LARGE` when the full pretty-printed prompt/schema was sent. The same request returned `200` SSE after compacting only valid JSON blocks inside the prompt fences (the instructions and schema values were unchanged). The Gateway Demo adapter now applies that compaction to map and reducer prompts and maps any remaining token-safety response to a stable localized error; it does not retry a rejected request.

## Local verification

- Lint and typecheck pass; lint retains two Fast Refresh warnings for the shared i18n module (no errors).
- Unit/integration: 60 files, 340 tests pass with no unhandled errors or React act warnings, including Gateway Demo session, SSE, batching, reducer, gate, cost, and redaction regressions.
- Build passes with 18 app-shell precache entries; the 1,162.92 kB main chunk / 351.16 kB gzip bundle-size warning remains.
- Full and production-only dependency audits report zero advisories.
- Final serial browser matrix: 55 passed, 2 skipped (non-Chromium update probes) across Chromium, Firefox, and WebKit, 57 cases total. All three engines passed the active wizard, Gateway Demo one-page and five-page map/reduce flows, five-locale shell switching, PWA offline shell, explicit versioned update (Chromium), stress, backup, interruption, large-page-count preview and bounded responsive navigation.

Detailed changes, exact limitations, official sources and the user-push acceptance checklist are in the [remediation report](docs/reviews/2026-09-08-production-remediation.md). The [original review](docs/reviews/2026-09-08-production-readiness.md) remains an unchanged historical baseline. [TASK.md](TASK.md) owns task status.

The earlier published Pages checkpoint `dd98943` passed its remote gate; the output-limit follow-up below records the later verified checkpoint and the remaining live failure. KB-MCP was consulted but returned no repository-specific acceptance evidence. No backend addition or architectural rewrite is needed; TASK-068 remains NO-GO until a real PDF map/reduce, quota/usage reconciliation, and target-device storage/cache evidence are recorded.

## Output-limit follow-up

The published prompt-compaction checkpoint `1b76da1` passed Actions run [34214400653](https://github.com/yapweijun1996/idp-benchmark-lab/actions/runs/34214400653) and Pages deployment. A fresh Pages-origin wizard run with the bundled Nexabyte PDF confirmed HTTP 200 but ended with `response.incomplete`, `incomplete_details.reason: max_output_tokens`, and usage of 3555 input / 800 output / 4355 total tokens. JSON stopped during item 9. This supersedes the earlier HTTP-only success inference: the complete Nexabyte schema is not proven to fit the demo output allowance. The frontend classifies this as a non-retryable output-limit failure and retains partial text, usage, timing and redacted attempt evidence. Full extraction remains externally blocked by the gateway output allowance unless the user explicitly chooses fewer fields or another provider. TASK-068 remains NO-GO.
